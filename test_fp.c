#include <stdio.h>

int main(void) {
    printf("0.1 + 0.2 = %.10f\n\n", 0.1f + 0.2f);

    printf("0.1 is represented as %.10f\n", 0.1f);
    printf("0.2 is represented as %.10f\n\n", 0.2f);
    
    return 0;
}
